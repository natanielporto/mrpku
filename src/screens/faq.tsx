import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export function FaqScreen() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>Faq secreen</Text>
    </SafeAreaView>
  );
}
